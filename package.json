{
  "name": "toolmanager-api",
  "version": "1.0.0",
  "main": "api/identify.js",
  "scripts": {
    "build": "echo 'No build step needed for API'"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "mongodb": "^6.3.0"
  },
  "engines": {
    "node": "18.x"
  }
}