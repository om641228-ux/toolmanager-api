{
  "name": "toolmanager-api",
  "version": "1.0.0",
  "main": "api/identify.js",
  "scripts": {
    "build": "mkdir -p public && echo 'API is Live' > public/index.html"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "mongodb": "^6.3.0"
  },
  "engines": {
    "node": "24.x"
  }
}
